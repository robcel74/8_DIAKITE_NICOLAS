<UserControl x:Class="GameOfThrones.UCJeu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GameOfThrones"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000"

             Focusable="True"
             Loaded="UserControl_Loaded"
             KeyDown="UserControl_KeyDown"
             KeyUp="UserControl_KeyUp"
             MouseDown="UserControl_MouseDown" Width="1000" Height="600">

    <Grid x:Name="MainGrid">
        <Image x:Name="imgMap" Height="600" Width="1000"/>
        <Canvas x:Name="GameCanvas" Background="Transparent" Width="1000" Height="600">
            <TextBlock x:Name="StatusText" Foreground="White" FontSize="20" Canvas.Left="10" Canvas.Top="10" Text="Vie Donjon: 100"/>
        </Canvas>
    </Grid>
</UserControl>
